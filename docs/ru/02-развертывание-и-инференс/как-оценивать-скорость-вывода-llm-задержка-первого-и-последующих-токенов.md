## 1. Введение
Если вы используете коммерческую большую модель или локально развертываете open source большую модель, то кроме качества генерации еще один ключевой показатель — скорость генерации token. Причем это не просто количество token в секунду, а два отдельных этапа:
- prefill: предзаполнение, параллельная обработка входных tokens;
- decoding: декодирование, генерация следующего token по одному.

## 2. Разные компании используют разные термины:
- Задержка первого token, Time To First Token (TTFT), prefill, Prefilling

   Все они означают задержку от ввода до вывода первого token;
- Задержка каждого output token (без первого Token), Time Per Output Token (TPOT)

  Означает скорость выдачи начиная со второго token;
- Latency

  Теоретически это время от ввода до вывода последнего token. Принципиальная формула расчета: Latency = (TTFT) + (TPOT) * (the number of tokens to be generated);
- Tokens Per Second (TPS):

  (the number of tokens to be generated) / Latency;


> Можно заметить, что генерация первого token отличается от генерации остальных tokens. Почему? Чтобы ускорить инференс!

## 3. Теоретическая скорость инференса большой модели
### Объем вычислений
Основной объем вычислений в процессе инференса большой модели приходится на decoder layer Transformer. Для каждого token и каждого параметра модели этот слой требует одну единицу unit вычислений, поэтому в процессе инференса на каждый token и каждый параметр модели требуется 1 unit × 2 flops = 2 операции с плавающей точкой.

У большой модели подавляющее большинство параметров находится в Transformer. В качестве оценки можно напрямую считать 2*параметры модели.

![alt text](../../../02-第二章-部署与推理/assest/如何评判大模型的输出速度？首Token延迟和其余Token延迟有什么不同？/1.PNG)

### Вычислительная база
Получив общую оценку объема вычислений, нужно дополнительно привязать ее к знакомой нам вычислительной мощности GPU. Для этого нужны сведения о вычислительной базе. Ниже приведено сравнение некоторых распространенных GPU:

![alt text](../../../02-第二章-部署与推理/assest/如何评判大模型的输出速度？首Token延迟和其余Token延迟有什么不同？/2.PNG)

### Утилизация GPU
FLOPS utilization оценивается исходя из максимальных значений, достижимых сейчас в индустрии:

![alt text](../../../02-第二章-部署与推理/assest/如何评判大模型的输出速度？首Token延迟和其余Token延迟有什么不同？/3.PNG)

### Формула расчета
Имея общую информацию о вычислительной мощности GPU, мы можем оценить скорость инференса:

![alt text](../../../02-第二章-部署与推理/assest/如何评判大模型的输出速度？首Token延迟和其余Token延迟有什么不同？/4.PNG)

Предположим, у нас есть 10 карт A100, используется точность BF16 (то есть одна карта имеет вычислительную мощность 312TFLOPS), утилизация GPU составляет 46.2%. Наша модель — Llama-3.1 70B, prompt равен 1K tokens, средняя скорость:
10*312T*46.2%/(2*70B*1K) = 10.3tokens/s, то есть в среднем выводится 10 tokens в секунду.

***Слишком медленно!***

## 4. Один из способов повысить скорость: KV Cache
KV Cache использует идею обмена пространства на время: повторно использует KV cache из предыдущего инференса, что может существенно снизить давление на память, повысить производительность инференса и при этом не влияет на точность вычислений.

В decoder-архитектуре главное — это стек структур self-attention в transformer. Суть KV-cache в том, что ранее вычисленные key-value и текущий query используются для генерации следующего token.

prefill означает, что при генерации первого token у kv еще нет никакого кэша: нужно заранее заполнить и закэшировать KV-матрицу, соответствующую prompt. Поэтому первый token генерируется медленнее всего, а начиная со второго token модель быстро обращается к кэшу и также кэширует kv предыдущего token.

Видно, что это схема обмена пространства на время: кэш постоянно растет. Поэтому при приватном развертывании и расчете видеопамяти, кроме размера модели, нужно учитывать размеры prompt и completion в вашем приложении (и, конечно, batch-size).

## 5.FAQ
Q: Если это матрицы QKV, почему Q не кэшируется?
A: Потому что при вычислении attention используемый Query — это только что сгенерированный одиночный token; в отличие от KV, которые содержат все Tokens, сгенерированные ранее. Значение KV, соответствующее этому token, также нужно закэшировать в текущем раунде.

## Ссылки

<div id="refer-anchor-1"></div>

[1] [GitHub: LLMForEverybody](https://github.com/luhengshiwo/LLMForEverybody)
